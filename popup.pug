doctype html
html( lang="en" )

  head
    meta(charset="UTF-8")
    // <link rel="icon" href="/favicon.ico">
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    script(type="text/javascript" src="popup.js")
    title Vite App

  body
    h1 Your final price is
    h2#try-price N/A
    h2#usd-price N/A
    .fee
      .inline
        .logo
          img(src="./assets/mostaql.png" width="20" height="20")
        .description Mostaql fee
      .red -20%
    .fee
      .inline
        .logo
          img(src="./assets/paypal.svg" width="20" height="20")
        .description PayPal fee
      .red -4%
    .fee
      .inline
        .logo
          img(src="./assets/paypal.svg" width="20" height="20")
        .description PayPal convertion to TRY fee
      .red -70₺
    style.
      * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
      Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue",
      sans-serif;
      }
      body {
      width: 400px;
      max-width: 400px;
      padding: 1rem;
      background-color: #181c2c;
      color: #45df74;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      }
      h1 {
      font-weight: normal;
      color: #cecece;
      margin-bottom: 0.2rem;
      }
      .description {
      color: #cecece;
      }
      .fee {
      width: 100%;
      max-width: 300px;
      display: flex;
      align-items: center;
      justify-content: center;
      }
      .logo {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 0.4rem;
      }
      .inline {
      width: 100%;
      display: flex;
      align-items: center;
      }
      .red {
      color: #df4578;
      }
      #try-price {
      font-size: 2.2rem;
      }
      #usd-price {
      font-size: 0.5rem;
      color: #cecece;
      }